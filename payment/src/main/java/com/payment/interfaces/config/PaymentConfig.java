package com.payment.interfaces.config;

import org.springframework.context.annotation.Configuration;

/**
 * 支付系统配置类
 * 
 * 可在此添加:
 * - 过滤器配置
 * - 拦截器配置
 * - 跨域配置
 * - 消息转换器配置
 */
@Configuration
public class PaymentConfig {
    
    // 可在此添加自定义Bean配置
}
